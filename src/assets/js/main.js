// Import jQuery and Plugins
import $ from 'jquery';
window.$ = window.jQuery = $;

window.App = {
	data: {},
	utils: {}
};

// Components
import Swiper from 'swiper';
window.Swiper = Swiper;

App.init = () => {

	// components

	// instance pages
	App.pages = {
	};

};

App.init();
